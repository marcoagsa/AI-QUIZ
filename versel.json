{
  "functions": {
    "api/*.ts": { "runtime": "nodejs18.x" }
  },
  "builds": [
    {
      "src": "angular.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "www"
      }
    },
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ]
}
